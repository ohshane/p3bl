# ─── Required ──────────────────────────────────────────────────────────
VITE_OPENROUTER_API_KEY=sk-or-v1-your-api-key-here
VITE_API_BASE=https://openrouter.ai/api/v1

# ─── Optional overrides ───────────────────────────────────────────────
# Base URL for generating QR codes / invite links.
# Defaults to window.location.origin in the browser.
# VITE_BASE_URL=https://yourdomain.com

# Override the chat WebSocket URL.
# Defaults to auto-detected: wss://<host>/ws/chat (production)
#                             ws://<host>/ws/chat  (development)
# VITE_WS_URL=wss://yourdomain.com/ws/chat

# Override the Yjs collaboration WebSocket URL.
# Defaults to auto-detected: wss://<host>/ws/yjs (production)
#                             ws://<host>/ws/yjs  (development)
# VITE_YJS_WS_URL=wss://yourdomain.com/ws/yjs

# Optional WebSocket debug logs in browser console.
# Enabled automatically in development, but can be forced with:
# VITE_WS_DEBUG=true
# VITE_YJS_DEBUG=true

# ─── Server-side (production only) ────────────────────────────────────
# PORT=3000
# Optional server WebSocket debug logs:
# WS_DEBUG=1
